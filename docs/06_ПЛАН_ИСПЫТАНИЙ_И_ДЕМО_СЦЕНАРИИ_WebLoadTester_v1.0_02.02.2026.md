# План испытаний и демонстрационные сценарии — WebLoadTester

**Версия:** v1.0 02.02.2026

## 1. Демонстрационные кейсы (перенос из 00)

(ниже — как “история для защиты”: ситуация → действие → ожидаемый результат)

5) Демонстрационные кейсы (простая реализация, сильная защита)
A) “Не открывается из внутренней сети” — диагностика показывает, на каком уровне сбой (DNS/TCP/TLS/HTTP).
B) “TLS сертификат истёк” — фиксируется причина TLS-ошибки и срок сертификата.
C) “Медленно грузится UI” — измеряются тайминги загрузки, сравниваются метрики.
D) “API отдаёт 500” — HTTP проверки фиксируют 5xx и детали по endpoint’ам.

---

## 2. Матрица испытаний (заполняемая)

| ID | Модуль | Сценарий | Входные данные | Ожидаемый результат | Доказательство (RunId / artifact) |
|---|---|---|---|---|---|
| T-A1-01 | A1 UI-сценарии | Базовая навигация и клики | URL + шаги | Success, скриншоты OnError |  |
| T-B1-01 | B1 HTTP функциональные | API отдаёт 500 | endpoint list | Failed, детали 5xx |  |
| T-C1-01 | C1 Сетевая диагностика | Не открывается из внутренней сети | host + port | Локализация DNS/TCP/TLS/HTTP |  |

---

## 3. Критерии успешности минимальной версии
- UI позволяет создать/сохранить/загрузить тест и профиль запуска.
- Прогон создаёт запись в БД и папку `/runs/{RunId}/`.
- `report.json` появляется всегда, содержит статус и метрики.
- HTML отчёт и Telegram не ломают прогон при ошибках (опционально).
