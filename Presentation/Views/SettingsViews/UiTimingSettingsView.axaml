<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="WebLoadTester.Presentation.Views.SettingsViews.UiTimingSettingsView">
    <ScrollViewer>
        <StackPanel Spacing="8">
            <TextBlock Text="UI Timing Settings" Classes="h1" />
            <TextBlock Text="URLs" Classes="label" />
            <ItemsControl ItemsSource="{Binding Urls}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding .}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <StackPanel>
                    <TextBlock Text="Repeats per URL" Classes="label" />
                    <TextBox Text="{Binding RepeatsPerUrl}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Concurrency" Classes="label" />
                    <TextBox Text="{Binding Concurrency}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Wait Until" Classes="label" />
                    <TextBox Text="{Binding WaitUntil}" />
                </StackPanel>
            </StackPanel>
            <CheckBox Content="Headless" IsChecked="{Binding Headless}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
